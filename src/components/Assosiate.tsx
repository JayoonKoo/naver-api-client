import useSearchState from "../hooks/useSearchState"

const Assosiate = () => {
	const state = useSearchState()
	console.log(state);
	

	return (
		<div>associate</div>
	)
}

export default Assosiate
